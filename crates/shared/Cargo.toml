[package]
name = "shared"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Core psychometric models, scoring calculations, datasets, and serialization formats for Revisited IPIP-NEO"

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
bson = { workspace = true, features = ["serde"] }
miniz_oxide = { workspace = true }
num = { workspace = true }
spodeian-export = { workspace = true }
spodeian-tokens = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }

[features]
default = ["std"]
std = ["serde/std", "serde_json/std", "num/std"]
